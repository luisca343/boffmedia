import { Replay } from "@/generated/api"
import { useRotomRequest } from "@/hooks/useRotomRequest"

export function useGetRepeticiones(uuid: string) {
  /*
  const { data, error, isLoading, refetch, setData } = useRotomRequest(
    battleService.getUserReplays,
    uuid,
  )*/

  return {
    //repeticiones: data,
    //error,
    //isLoading,
    //refetch,
    //setRepeticiones: setData,
  }
}

