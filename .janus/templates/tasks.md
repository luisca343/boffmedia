# Tasks — {{TITLE}}

> Spec: `{{ID}}` · Generated {{DATE}}
>
> Format: `- [ ] TNNN Title — files: <globs> — depends: <ids|none>`
> States: `[ ]` pending · `[~]` in-progress · `[x]` done · `[-]` blocked
> Mark parallelizable tasks with `[P]` after the id.

## Tasks

<!-- Decompose plan.md into ordered, executable tasks. Example:
- [ ] T001 Create users table schema — files: src/db/schema.ts — depends: none
- [ ] T002 [P] Auth service with JWT — files: src/auth/** — depends: T001
-->
