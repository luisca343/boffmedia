---
description: Show the SDD dashboard
---

Run `janus status` and summarize: active specs, task progress, recent run outcomes,
and anything needing attention (blocked tasks, failed runs). Use `janus analyze status`
to note whether the architecture brief is stale.
