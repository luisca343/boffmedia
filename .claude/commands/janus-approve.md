---
description: Approve a spec (human gate — confirm with the user first)
---

The user wants to approve spec "$ARGUMENTS".

Approval is a human decision. Confirm the user intends to approve, then run
`janus spec approve $ARGUMENTS`. After approval, suggest `janus plan new $ARGUMENTS`
then `janus tasks generate $ARGUMENTS`.
