feature: gremio-mazmorra

tasks[8]{id,rf,status,ref,files,accept,size,desc}:
  T1,RF-06,done,frontend-direct,apps/web/locales/en/smartrotom/misiones.json;apps/web/locales/es/smartrotom/misiones.json,"misiones.json (en+es) gains a mazmorra namespace (title/subtitle/leaderboard.*/personalRecord.* incl. both empty states) and the existing placeholder sideRail namespace is replaced with real copy (brand; tagline; eyebrow; ariaLabel; footer; tabs.{board story map backpack log mazmorra}.{label short}) - both files stay valid JSON with identical key shape",M,"Add misiones.mazmorra.* keys (leaderboard copy + empty state; personal-record copy + 404 empty state) and lift SideRail/MobileRail hardcoded Spanish into misiones.sideRail.* (brand text 'Gremio de Exploradores' replacing 'Misiones'; existing eyebrow/tagline/footer/aria-label; per-tab label+short incl. new mazmorra tab) in both en and es locale files"
  T2,RF-01;RF-03;RF-04,done,frontend-direct,apps/web/src/app/smartrotom/misiones/_hooks/queries.ts,"useDungeonRanking() and useDungeonPlayerStats() are exported from queries.ts; ranking hook wraps MazmorraService.getRanking behind a stable query key; stats hook is keyed by useRotomUuid() and 'enabled' only when a uuid exists (mirrors useArcadeStreak's enabled-on-uuid pattern in arcade/_hooks/queries.ts) so signed-out / 404 resolve to an empty result rather than throwing into render",S,"Add the two data hooks next to the existing useQuestSystem in misiones/_hooks/queries.ts calling MazmorraService.getRanking(limit) and MazmorraService.getPlayerStats(uuid) via @tanstack/react-query"
  T3,RF-01;RF-02;RF-06,done,frontend-direct,apps/web/src/app/smartrotom/misiones/_components/MazmorraLeaderboard.tsx,"Renders ranking rows in ms-* tavern classes (Paper/Label/ui primitives only - no bg-layer-1/edge classes from the deleted stub); rows 1-3 get a medal treatment built only from existing ms-gold-* tokens; the row whose uuid matches useRotomUuid() is visually highlighted; an empty array renders EmptyBoard with a translated empty-leaderboard message instead of an empty table; every literal string routes through useTranslations",M,"New leaderboard panel component props-driven on the ranking array + current uuid; styled like the sibling Paper-based boards (KingdomMap/ThreadWall) not the deleted MenuWrapper stub"
  T4,RF-03;RF-04;RF-06,done,frontend-direct,apps/web/src/app/smartrotom/misiones/_components/MazmorraRecord.tsx,"When useDungeonPlayerStats resolves shows mejorPartida's floors/stage/clear-time/curses/rank summary in an ms-* card (Paper + Label + stat-tablet grid like PlayerHeader's); when uuid is null or the call 404s shows a themed empty state via EmptyBoard/Paper (never a raw error string or broken layout); every literal string routes through useTranslations",M,"New personal-record panel component consuming useDungeonPlayerStats(); mirrors PlayerHeader's stat-tablet grid pattern for the mejorPartida fields"
  T5,RF-01;RF-02;RF-03;RF-04;RF-05;RF-06,done,frontend-direct,apps/web/src/app/smartrotom/misiones/mazmorra/page.tsx,"Visiting /smartrotom/misiones/mazmorra inside the existing misiones/layout.tsx Tavern shell renders a heading then MazmorraLeaderboard then MazmorraRecord stacked; page is a 'use client' component matching every sibling screen's convention (BoardLoading/BoardError reused where relevant); root fits the shell's h-[calc(100dvh_-_3rem)] contract (never h-full); no bg-layer-1/edge classes anywhere in the tree",M,"Wire the two new panels into a single route reusing sibling screens' heading/Label/Divider conventions (see mapa/page.tsx and bitacora/page.tsx) - two panels stacked: leaderboard then personal record"
  T6,RF-05;RF-06,done,frontend-direct,apps/web/src/app/smartrotom/misiones/_components/SideRail.tsx,"SECTIONS has 6 entries incl. a new dungeon entry (href '/smartrotom/misiones/mazmorra') placed last; every SECTIONS label/short plus SideRail's eyebrow/h1-brand/tagline/nav-aria-label/footer text plus MobileRail's brand span all resolve via useTranslations('misiones.sideRail') reading the keys added in T1 (no literal Spanish left in the file); isCurrent() highlights the new tab exactly like the other 5 when /smartrotom/misiones/mazmorra is open",M,"Add the 6th SECTIONS entry for the dungeon screen and lift every hardcoded string in SideRail+MobileRail (SECTIONS array AND the standalone brand/eyebrow/tagline/footer/aria-label strings) to i18n; rebrand the brand text from 'Misiones' to 'Gremio de Exploradores'"
  T7,RF-07,done,frontend-direct,apps/web/src/app/smartrotom/mazmorra/page.tsx;apps/web/src/app/smartrotom/mazmorra/ranking/page.tsx;apps/web/src/app/smartrotom/mazmorra/_components/MenuWrapper.tsx,"apps/web/src/app/smartrotom/mazmorra/ no longer exists (directory removed entirely) so /smartrotom/mazmorra and /smartrotom/mazmorra/ranking 404 instead of resolving to the old redirect/table",S,"Delete the superseded Boffmedia-styled stub now that the ms-* screen replaces it"
  T8,RF-08,done,frontend-direct,none (verification-only step - no files touched),"pnpm type-check exits 0 with no new errors introduced by this feature's files",S,"Final build gate per repo convention (next.config.mjs ignores TS build errors so this is the only real type gate)"
